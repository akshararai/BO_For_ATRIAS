function [fit1, fit2, score, score_4d, simout_vec] = fake_kernel_scores_NM_mismatch(...
    param, stopTime, rtp, cost_op, siminit_name)
    fit1 = input('Enter cost: ');
    fit2 = fit1;
    score = 0;
    score_4d = 0;
    simout_vec = [];
end
